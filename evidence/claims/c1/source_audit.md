# Claim 1 source audit

Source: ar5iv rendering of arXiv:2509.04477, retrieved 2026-07-28 with an
explicit browser User-Agent. SHA-256:
`238da228cfbb14fed2b33b919a294042e79953d3bbae2ca8b8957ac1a716950a`.

Section V assumes compact Euclidean `X` and `Y` and a locally Lipschitz
surplus. Theorem 1 states uniform-norm density of finitely `Y`-convex
functions in all `Y`-convex functions. Its quantifiers are: for every
`epsilon>0` and every `f` in `C^Y(X)`, there is a finite support
`Y_tilde subset Y` whose generalized biconjugate approximates `f` uniformly
within `epsilon`.

The certificate uses source anchors `#S5.p3`, `#S5.SS1`, and
`#Thmtheorem1`. It audits the zero-Lipschitz case separately and does not infer
the universal statement from a finite grid.
