# Claim 6 evaluation

Pending the committed Hugging Face cpu-upgrade run.

The verifier will report exactly `VERIFIED` or `BLOCKED`; it never converts a
failed proof step, a numerical special case, or a missing assumption into a
pass. The constructed multidimensional cases corroborate the map formula but
are not treated as proof of the universal implication; that role belongs to
the symbolic certificate.
